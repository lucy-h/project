<%= form_for :session, :url => signup_path, :method => :post do |f| %>

	<%= f.label "Email address" %>
	<%= f.text_field :email %>

	<div class="actions">
		<br/>
		<%= f.submit "Log in" %>
	</div>

<% end %>